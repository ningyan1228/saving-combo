# 「省钱组合」开发需求文档 V1.0

## 1. 项目定位

**产品名：** 省钱组合  
**副标题：** 你的数字生活自动省钱系统  
**产品形态：** 中文 Web 应用，先做响应式网页，后续再考虑小程序/App。  
**核心承诺：** 用户不用每天自己搜价格、返现和优惠。系统替用户发现机会、判断是否适用、计算实际成本，并在值得操作时提醒。

省钱组合不是泛资讯站，也不是信用卡代办或代付平台。它是一个面向数字订阅用户的“个人机会雷达”。

### 首批聚焦场景

1. AI 订阅：ChatGPT、Claude、Gemini、Cursor 等。
2. 数字工具：AI API、生产力 SaaS、云服务等。
3. 付款成本：官方价格、地区价格、汇率、公开返现与活动。
4. 续费决策：临近续费时告诉用户，是否存在更合适的官方方案或公开优惠。

### 不做的事（V1 硬约束）

- 不收集银行卡号、CVV、密码、身份证、验证码或支付凭据。
- 不做代付、换汇、资金托管、贷款、理财建议或“保证返现”。
- 不绕过登录、反爬、付费墙或平台访问限制；只使用官方公开页、公开 RSS/API、用户主动提交的公开链接与人工可核验资料。
- 不承诺“实时最低价”或“必定可用”。每条机会都必须显示来源、验证时间、适用条件和风险提示。
- 不把 OpenAI 或其他第三方 API Key 放入前端代码、GitHub 仓库或 GitHub Pages。

---

## 2. 目标用户与产品价值

### 目标用户

- 经常订阅 ChatGPT、Claude、Cursor、Gemini 或其他海外数字工具的中文用户。
- 想知道不同付款方式、汇率和返现后到底花了多少钱的人。
- 有多个订阅，不想错过续费前活动或优惠窗口的人。
- 不想看大量“羊毛资讯”，只想看自己真正能用的机会的人。

### 用户完成的核心任务

用户首次进入后，填写：所在地区、常用结算币种、持有卡的“卡组织/产品名称”、关注的订阅服务、已有订阅及下次续费日期。

系统返回：

> “针对你的组合，当前有 2 条可关注机会；其中 1 条适用于你的 ChatGPT Plus 支付，预计可减少约 ¥X 成本。下一次续费在 X 月 X 日，建议提前 X 天查看。”

这里的“可减少”必须是透明计算结果，展示计算公式与条件，不可夸张营销。

---

## 3. V1 成功标准与边界

### V1 验证目标

用最低成本验证三件事：

1. 用户是否愿意创建自己的“省钱组合”。
2. 用户是否愿意订阅提醒，而不是只看一次价格。
3. 用户是否愿意为更精确的匹配、续费追踪和提醒付费。

### V1 最小闭环

```text
首页 → 创建省钱组合 → 查看匹配机会 → 收藏/订阅提醒
→ 添加一个订阅续费日 → 到期前提醒 → 打开机会详情并完成操作
```

### 首个垂直切片

只先做好 **ChatGPT Plus 的实际支付成本与公开返现机会**，再扩展 Claude、Cursor、Gemini。

V1 不做大而全的“所有信用卡、所有国家、所有优惠”。第一批只录入人工核验过的服务、地区和活动。每新增一个服务或活动，必须有可追溯的来源。

---

## 4. 信息架构与页面

### 4.1 首页 `/`

**目的：** 让新用户在 10 秒内理解价值，并开始建立组合。

模块：

1. 首屏标题：`不用到处搜优惠，省钱组合替你盯着。`
2. 副文案：`自动追踪 AI 订阅价格、公开返现、汇率和续费节点，只推你可能用得上的机会。`
3. 主按钮：`创建我的省钱组合`
4. 次按钮：`先看 ChatGPT Plus 成本`
5. 今日机会卡（仅展示人工审核通过的 3–6 条）
6. 成本计算示例：官方标价 → 汇率成本 → 已知返现 → 预计实付
7. “为什么可信”：显示来源链接、最后核验时间、适用条件、非保证说明
8. 常见问题：地区限制、卡活动资格、汇率波动、数据与隐私说明。

### 4.2 机会大厅 `/opportunities`

**目的：** 浏览公开、已核验的机会。

筛选：服务、地区、卡组织、机会类型（价格变化/返现/学生优惠/限时活动/续费建议）、有效状态。

每张机会卡显示：

- 机会标题
- 适用服务
- 适用地区与付款条件
- 预计节省（仅在计算依据足够时展示）
- 截止时间或“未公布截止时间”
- 来源与最后核验时间
- 可信等级：官方 / 合作方公开页 / 用户报料待核验
- `查看详情`、`加入我的提醒`

### 4.3 机会详情 `/opportunities/:slug`

必须包含：

- 一句话结论：适不适合什么人
- 适用条件清单
- 逐步操作说明（只说明官方、合规操作）
- 成本计算器与公式
- 原始来源链接
- 首次发现、最后核验、失效状态
- 风险提示：活动资格最终以发卡方/服务方为准
- 用户反馈：`我已使用` / `不适用` / `信息失效`，需说明原因

### 4.4 我的组合 `/my-combo`

未登录用户可先本地填写并预览；登录后同步。

字段：

- 所在国家/地区
- 默认货币（CNY/USD 等）
- 关注服务（多选）
- 已订阅服务：服务名、套餐、账单币种、标价、下次续费日
- 支付方式标签：卡组织（Visa/Mastercard 等）、卡产品名称、发卡地区
- 提醒偏好：站内提醒、浏览器通知、邮件提醒（V1 先实现站内提醒与邮件）

隐私说明：不填写完整卡号；卡产品名称可模糊填写；用户可随时删除资料与账户。

### 4.5 成本计算器 `/calculator`

第一版固定支持 ChatGPT Plus，并预留服务配置化能力。

输入：服务、标价、币种、付款地区、汇率来源、可能的返现比例/固定返现。

输出：

```text
实际支付成本 = 标价 × 汇率 + 已知支付手续费 − 可确认返现
```

必须同时显示：

- 标价来源与抓取/录入时间
- 汇率来源与更新时间
- 返现是否已验证、上限、门槛、名额、活动截止时间
- “预计”而非“保证”的提示

### 4.6 订阅日历 `/subscriptions`

显示用户已添加订阅的下次续费日、预计成本、已有匹配机会、提醒状态。

提醒节奏：续费前 14 天、7 天、1 天。用户可以关闭某个订阅的提醒。

### 4.7 节省账本 `/savings`

V1 只记录用户主动确认的结果，不自动虚构节省金额。

字段：服务、发生日期、原价、实际成本、节省金额、关联机会、用户备注、是否已验证。

首页可展示匿名聚合数据，例如“本月用户已主动记录节省 ¥X”，但没有足够数据时不展示。

### 4.8 管理后台 `/admin`

仅管理员可访问。用于：

- 新建和编辑服务、价格方案、活动、来源、地区规则。
- 审核用户报料。
- 标记机会为有效、即将失效、已失效。
- 查看采集任务失败、来源页面变化、待人工审核队列。
- 手动触发重新计算匹配结果。

---

## 5. 自动化系统设计

自动化的目标不是“全网爬虫”，而是把可信来源变成稳定的数据流水线。

### 5.1 数据来源优先级

| 优先级 | 来源 | 用法 |
|---|---|---|
| A | 服务商官方价格页、官方公告、银行/卡组织官方活动页 | 自动检查变化；优先作为正式机会来源 |
| B | 官方 RSS、公开 API、授权合作数据 | 定时同步并标准化 |
| C | 用户报料、公开媒体报道 | 进入待审核队列，不能直接推送为“可用机会” |

每条数据必须保存来源 URL、抓取/录入时间、原文摘要或哈希、负责人、审核状态。

### 5.2 定时任务

1. `source-check`：每日检查官方价格和活动来源是否有变化。
2. `exchange-rate-sync`：每日更新汇率；页面显示“更新时间”，失败时保留最后一次成功数据并提示。
3. `opportunity-expiry-check`：每日将过期活动标记为“已失效/待复核”。
4. `match-rebuild`：当用户组合、活动、价格、汇率变化后，重新匹配。
5. `renewal-reminder`：每天扫描续费日在 14/7/1 天后的用户，生成提醒。
6. `report-triage`：用户报料进入待审核队列；AI 只能辅助提取字段和生成摘要，不能直接发布。

### 5.3 匹配逻辑（V1）

一条机会能推给用户，需要同时满足：

```text
机会有效
AND 服务匹配
AND 地区条件匹配
AND 用户的付款方式标签匹配（若活动有此条件）
AND 该用户没有主动隐藏该机会
```

`estimated_saving` 的计算只在标价、汇率、返现规则均完整时执行；任何一个条件缺失时显示“待确认”，不显示具体节省金额。

### 5.4 防止信息失真

- 价格变化、返现规则、有效期不能只由 AI 判断。
- AI 输出必须由规则校验：日期格式、金额、币种、来源域名、必填条件。
- 正式推送只使用管理员审核为 `verified` 的数据。
- 任何来源连续检查失败或正文结构大变时，自动降为 `needs_review`，暂停主动推荐。

---

## 6. 数据模型（Supabase / PostgreSQL）

### 必要表

| 表名 | 关键字段 | 用途 |
|---|---|---|
| `profiles` | user_id, country, default_currency, notification_preferences | 用户偏好 |
| `services` | id, slug, name, category, official_url, active | 服务目录 |
| `service_plans` | service_id, name, list_price, currency, billing_cycle, source_url, verified_at | 官方套餐与价格 |
| `payment_methods` | user_id, network, product_name, issuer_region, active | 用户的非敏感支付标签 |
| `subscriptions` | user_id, service_plan_id, amount, currency, renewal_date, active | 用户订阅与续费日 |
| `opportunities` | id, slug, title, type, status, start_at, end_at, source_url, verified_at, summary | 公开机会主体 |
| `opportunity_rules` | opportunity_id, service_id, regions, card_networks, min_spend, cashback_type, cashback_value, cap_amount | 匹配与计算条件 |
| `source_registry` | id, name, url, source_type, check_interval, last_checked_at, status | 来源登记 |
| `source_snapshots` | source_id, content_hash, captured_at, parsed_data, change_type | 变化追踪 |
| `opportunity_matches` | user_id, opportunity_id, status, estimated_saving, calculated_at | 用户匹配结果 |
| `notifications` | user_id, type, title, body, scheduled_at, sent_at, status | 站内/邮件提醒 |
| `savings_entries` | user_id, subscription_id, opportunity_id, original_cost, actual_cost, saved_amount, occurred_at | 用户确认的节省记录 |
| `user_reports` | user_id, url, content, status, reviewer_note, created_at | 用户报料 |
| `audit_logs` | actor_id, action, entity_type, entity_id, before_data, after_data, created_at | 后台审核记录 |

### 权限要求

- Supabase Row Level Security：普通用户只能读取和修改自己的 `profiles`、`payment_methods`、`subscriptions`、`notifications`、`savings_entries`、`user_reports`。
- 已审核的 `services`、`service_plans`、`opportunities` 可公开读取。
- 管理后台写入使用服务端权限，绝不在浏览器暴露 service role key。

---

## 7. 技术架构

### 7.1 固定技术选择

| 层级 | 技术 | 原则 |
|---|---|---|
| 前端 | Next.js + TypeScript + Tailwind CSS | 导出静态站点，部署到 GitHub Pages |
| 数据与登录 | Supabase Free Tier（Auth + PostgreSQL） | 承担账号、结构化数据和权限，不在个人服务器保存用户文件 |
| 轻量后端 | 现有个人小服务器，Node.js 20 + TypeScript + Express | 只做 API 代理、定时任务、解析、通知与密钥保护 |
| 任务调度 | GitHub Actions 触发 + 服务器任务端点，或服务器 cron | 任务可观测、可重试、有失败告警 |
| 抓取/解析 | fetch + Cheerio；优先 RSS/API | 不默认使用 Puppeteer/Playwright；不绕过站点限制 |
| 部署 | GitHub Pages（前端）+ 个人服务器（API） | 前后端域名与 CORS 白名单明确 |

### 7.2 架构流程

```text
官方公开来源 / RSS / API / 用户报料
            ↓
服务器：采集、解析、变化检测、规则校验
            ↓
Supabase：结构化数据、审核状态、用户组合
            ↓
服务器：匹配、成本计算、续费提醒
            ↓
GitHub Pages 前端：机会、计算器、日历、账本
```

### 7.3 环境变量

仅放在服务器或 GitHub Actions Secret：

```text
SUPABASE_URL
SUPABASE_ANON_KEY                 # 前端可见，仅配合 RLS
SUPABASE_SERVICE_ROLE_KEY         # 仅服务器端
OPENAI_API_KEY                    # 仅服务器端，可选
EMAIL_PROVIDER_API_KEY             # 仅服务器端
ADMIN_EMAILS
ALLOWED_ORIGINS
```

仓库中只能提交 `.env.example`，不可提交真实密钥、用户数据、API 响应缓存或管理员账号。

---

## 8. 接口设计（第一版）

### 前端公开接口

```text
GET  /api/v1/services
GET  /api/v1/services/:slug/plans
GET  /api/v1/opportunities?service=&region=&type=&status=active
GET  /api/v1/opportunities/:slug
GET  /api/v1/exchange-rates?base=USD&quote=CNY
POST /api/v1/calculations/cost
```

### 登录用户接口

```text
GET    /api/v1/me/combo
PUT    /api/v1/me/profile
POST   /api/v1/me/payment-methods
DELETE /api/v1/me/payment-methods/:id
POST   /api/v1/me/subscriptions
PATCH  /api/v1/me/subscriptions/:id
DELETE /api/v1/me/subscriptions/:id
GET    /api/v1/me/matches
POST   /api/v1/me/matches/:id/save
GET    /api/v1/me/notifications
POST   /api/v1/me/savings
POST   /api/v1/reports
```

### 管理员接口

```text
POST /api/v1/admin/sources/check
POST /api/v1/admin/opportunities
PATCH /api/v1/admin/opportunities/:id
POST /api/v1/admin/reports/:id/review
POST /api/v1/admin/matches/rebuild
GET  /api/v1/admin/jobs
```

所有写接口必须做身份验证、输入校验、限流、审计日志；公开接口要缓存并限制高频请求。

---

## 9. UI 与交互要求

### 视觉方向

- 中文、简洁、有“金融工具感”但不做夸张财富承诺。
- 主色建议深海军蓝 + 清透薄荷绿；重点金额可用温和金色点缀。
- 信息卡片清晰、留白足、移动端优先。
- 不使用“稳赚”“暴富”“零风险”“官方担保”等词。

### 三个关键体验

1. **首次配置小于 2 分钟**：用逐步选择，不做一大张复杂表单。
2. **机会解释得明白**：每个推荐都必须回答“为什么推给我、要满足什么条件、预计省多少、来源是什么”。
3. **提醒不打扰**：默认只提醒高相关机会和订阅节点；用户可精确关闭。

### 空状态文案示例

- 我的组合为空：`先告诉我你常用什么服务，我来替你盯着。`
- 当前无匹配：`今天暂时没有适合你的新机会。我们仍会继续检查。`
- 活动待核验：`这条信息正在核验中，暂不建议据此操作。`

---

## 10. 商业化设计

### 免费层

- 浏览已审核机会。
- ChatGPT Plus 基础成本计算。
- 创建 1 个订阅、基础站内提醒。

### 付费层（验证后再开）

建议沿用低门槛路径：`免费试用 → 9.9 元首单 → 19.9 元升级`。

- 9.9 元：完整省钱组合、多个订阅、续费前提醒、机会收藏。
- 19.9 元：高级匹配、历史成本走势、节省账本、优先提醒。

先不开发复杂支付商城。付费前先验证：20 位付费用户、5 条公开正向反馈、月订单超过 100 后，再进入放大阶段。

### 合规合作收入（后期）

只有在来源和关系明确披露的情况下，才可接入工具推广分佣或合作活动。合作内容必须标识“推广/合作”，且不能影响机会排序的透明度。

---

## 11. 开发分期与验收

### P0：项目骨架（1–2 天）

- Next.js + TypeScript + Tailwind 初始化。
- GitHub Pages 静态部署跑通。
- Supabase 项目、表结构、RLS、基础 Auth 跑通。
- Express API 健康检查、CORS、环境变量、日志与部署说明。

**验收：** GitHub Pages 能访问；匿名用户能打开首页与公开机会；前端无秘密密钥。

### P1：首个可玩垂直切片（3–5 天）

- 只上线 ChatGPT Plus。
- 机会大厅、机会详情、成本计算器、我的组合向导。
- 管理后台人工录入活动与价格。
- 创建 1 个订阅并生成 14/7/1 天的站内提醒。

**验收：** 一名测试用户可完成“创建组合 → 得到匹配 → 添加续费日 → 看见提醒”的闭环；每个机会都有来源、验证时间和条件。

### P2：自动化与质量（3–5 天）

- 官方价格/活动来源登记与定时检查。
- 变化快照、待审核队列、失效自动处理。
- 邮件提醒。
- 用户报料、管理员审核、审计日志。

**验收：** 至少 3 个官方来源能被稳定检查；解析失败不会错误发布；过期活动不会继续主动推荐。

### P3：扩展与商业验证（验证数据达标后）

- 增加 Claude、Cursor、Gemini。
- 收藏、节省账本、更多地区/支付规则。
- 9.9 / 19.9 会员功能开关与支付接入。
- 数据仪表盘：激活用户、组合创建率、提醒打开率、转化率、用户确认节省额。

---

## 12. 必须交付的工程文件

1. `README.md`：本地启动、环境变量、部署、常见故障。
2. `.env.example`：只有变量名，无真实值。
3. `docs/architecture.md`：架构和数据流。
4. `docs/data-sources.md`：每个来源、允许用途、检查频率、失效处理。
5. `docs/admin-review.md`：审核标准和失效处理流程。
6. `supabase/migrations/`：完整可执行的表结构、索引、RLS 策略。
7. `scripts/`：数据初始化、来源检查、任务执行脚本。
8. 基础测试：成本计算、机会匹配、有效期判断、权限隔离。

---

## 13. 给 Codex 的执行要求

请先实现 P0 和 P1，不要跳到支付、复杂会员、全量爬虫或大量服务扩展。

开发顺序必须是：

```text
静态页面与数据模型
→ ChatGPT Plus 成本计算
→ 我的组合
→ 机会匹配
→ 订阅提醒
→ 管理员人工审核
→ 自动化来源检查
```

每完成一个阶段，都要提供：可访问地址、测试步骤、已知限制、下一阶段建议。遇到无法公开、无法合法验证或需要登录的数据源，不要尝试绕过；在后台标记为“暂不支持”，等待人工或授权数据接入。

