# 管理员审核流程

1. 核对来源是否公开、合法、可访问，记录来源 URL 与核验时间。
2. 明确服务、地区、付款条件、门槛、上限、截止日期和风险提示。
3. 不完整、歧义或需登录验证的信息保持 `needs_review`；不可直接推送。
4. 完成核验后标记 `verified`，并记录审计日志。
5. 来源检查失败、正文结构显著变化或活动到期时，改为 `needs_review` 或 `expired`，停止主动匹配。
